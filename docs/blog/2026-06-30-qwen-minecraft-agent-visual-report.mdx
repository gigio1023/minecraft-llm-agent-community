---
slug: natural-village-model-comparison
title: "Did the NPC Establish a Useful Minecraft Work Point?"
description: "A goal-oriented visual field report from a Minecraft natural-village harness run."
authors: [gigio1023]
tags: [minecraft, llm-agents, visual-report, evaluation, mineflayer]
image: /img/natural-village-model-report/hero-village-start.png
---

import heroVillage from '@site/static/img/natural-village-model-report/hero-village-start.png';
import qwenPlusImage from '@site/static/img/natural-village-model-report/qwen-plus-cycle30-follow.png';
import qwenMaxImage from '@site/static/img/natural-village-model-report/qwen-max-cycle20-follow.png';
import qwenMaxFirstPerson from '@site/static/img/natural-village-model-report/qwen-max-cycle30-first-person.png';
import gptMiniImage from '@site/static/img/natural-village-model-report/gpt54-mini-cycle30-follow.png';
import geminiImage from '@site/static/img/natural-village-model-report/gemini31-flash-lite-cycle30-follow.png';
import oakLogIcon from '@site/static/img/natural-village-model-report/icons/oak_log.png';
import oakPlanksIcon from '@site/static/img/natural-village-model-report/icons/oak_planks.png';
import craftingTableIcon from '@site/static/img/natural-village-model-report/icons/crafting_table.png';
import stickIcon from '@site/static/img/natural-village-model-report/icons/stick.png';
import woodenPickaxeIcon from '@site/static/img/natural-village-model-report/icons/wooden_pickaxe.png';
import cobblestoneIcon from '@site/static/img/natural-village-model-report/icons/cobblestone.png';
import chestIcon from '@site/static/img/natural-village-model-report/icons/chest.png';

export const goalSteps = [
  {level: 0, label: 'Village start', icon: chestIcon},
  {level: 1, label: 'Wood acquired', icon: oakLogIcon},
  {level: 2, label: 'Basic materials', icon: oakPlanksIcon},
  {level: 3, label: 'Crafting table', icon: craftingTableIcon},
  {level: 4, label: 'Tool retained', icon: woodenPickaxeIcon},
  {level: 5, label: 'Stone follow-through', icon: cobblestoneIcon}
];

export const runs = [
  {
    id: 'qwen-plus',
    label: 'Qwen Plus',
    model: 'Qwen Plus via ModelScope API',
    provider: 'ModelScope',
    image: qwenPlusImage,
    observed: 12,
    blocked: 8,
    noProgress: 10,
    requests: 38,
    tokens: 1129990,
    reqPerObserved: 3.17,
    tokensPerObserved: 94166,
    goalScore: 4,
    goalVerdict: 'Reached a usable wood-level work point.',
    goalSummary: 'It built the wood -> table -> pickaxe chain, then ended without stone follow-through.',
    finalGoalState: 'Crafting table progress plus wooden pickaxe retained; no cobblestone.',
    blockerRead: 'Some blocked/no-progress cycles remained, but the final material state stayed useful.',
    milestone: 'Workbench recorded at cycle 0004, wooden pickaxe retained, no cobblestone.',
    inventory: 'cherry_planks x2, oak_log x3, oak_planks x1, stick x2, wooden_pickaxe x1',
    inventoryItems: [
      {icon: oakPlanksIcon, label: 'planks', qty: 3},
      {icon: oakLogIcon, label: 'logs', qty: 3},
      {icon: stickIcon, label: 'sticks', qty: 2},
      {icon: woodenPickaxeIcon, label: 'wooden pickaxe', qty: 1}
    ],
    read: 'A clean early material setup lane: logs, planks, table placement, and a retained pickaxe chain.'
  },
  {
    id: 'qwen-max',
    label: 'Qwen Max',
    model: 'Qwen Max via ModelScope API',
    provider: 'ModelScope',
    image: qwenMaxImage,
    observed: 15,
    blocked: 8,
    noProgress: 7,
    requests: 38,
    tokens: 1093021,
    reqPerObserved: 2.53,
    tokensPerObserved: 72868,
    goalScore: 5,
    goalVerdict: 'Reached the deepest practical goal state.',
    goalSummary: 'It kept the wood -> table -> pickaxe chain and continued into stone material work.',
    finalGoalState: 'Wooden pickaxe retained and 6 cobblestone recorded at the end.',
    blockerRead: 'It still had blocked cycles and a final camera caveat, but the material chain continued.',
    milestone: 'Workbench recorded at cycle 0003, wooden pickaxe retained, 6 cobblestone recorded.',
    inventory: 'cherry_planks x1, cobblestone x6, dirt x4, stick x6, wooden_pickaxe x1',
    inventoryItems: [
      {icon: oakPlanksIcon, label: 'planks', qty: 1},
      {icon: stickIcon, label: 'sticks', qty: 6},
      {icon: woodenPickaxeIcon, label: 'wooden pickaxe', qty: 1},
      {icon: cobblestoneIcon, label: 'cobblestone', qty: 6}
    ],
    read: 'The deepest material chain in this run: it moved from wood and table work into pickaxe and stone work.'
  },
  {
    id: 'gpt54-mini',
    label: 'GPT-5.4 mini',
    model: 'gpt-5.4-mini',
    provider: 'OpenAI API',
    image: gptMiniImage,
    observed: 8,
    blocked: 9,
    noProgress: 13,
    requests: 204,
    tokens: 1115823,
    reqPerObserved: 25.5,
    tokensPerObserved: 139478,
    goalScore: 3,
    goalVerdict: 'Reached materials and held a table, but did not settle the work point.',
    goalSummary: 'It gathered useful materials and held a crafting table, then failed to close the table/work-point state.',
    finalGoalState: 'Crafting table in inventory, but no retained tool or stone follow-through.',
    blockerRead: 'Movement recovery displaced the practical goal after early crafting progress.',
    milestone: 'Crafting table held in inventory, but not placed; no pickaxe or cobblestone.',
    inventory: 'crafting_table x1, dirt x3, oak_log x3, oak_planks x6, oak_stairs x1, pink_petals x2, stick x4',
    inventoryItems: [
      {icon: craftingTableIcon, label: 'crafting table', qty: 1},
      {icon: oakLogIcon, label: 'logs', qty: 3},
      {icon: oakPlanksIcon, label: 'planks', qty: 6},
      {icon: stickIcon, label: 'sticks', qty: 4}
    ],
    read: 'The lane completed, but it spent far more provider records and did not close the same workbench step.'
  },
  {
    id: 'gemini-31',
    label: 'Gemini 3.1 Flash Lite',
    model: 'gemini-3.1-flash-lite',
    provider: 'Gemini API',
    image: geminiImage,
    observed: 11,
    blocked: 19,
    noProgress: 0,
    requests: 67,
    tokens: 1709204,
    reqPerObserved: 6.09,
    tokensPerObserved: 155382,
    goalScore: 3,
    goalVerdict: 'Reached table-level progress, then stalled around recovery.',
    goalSummary: 'It reached planks, sticks, and a table step, but the second half was dominated by blocked recovery.',
    finalGoalState: 'Logs, planks, and sticks retained; no tool or stone follow-through.',
    blockerRead: 'Blocked movement and generated-action rejection dominated the second half.',
    milestone: 'Workbench recorded at cycle 0005, sticks and planks retained, no shelter or storage.',
    inventory: 'oak_log x1, oak_planks x4, stick x8',
    inventoryItems: [
      {icon: oakLogIcon, label: 'logs', qty: 1},
      {icon: oakPlanksIcon, label: 'planks', qty: 4},
      {icon: stickIcon, label: 'sticks', qty: 8}
    ],
    read: 'The paced lane finished the 30 cycles, then spent much of the second half blocked around table movement.'
  }
];

export const materialEvents = {
  'qwen-plus': [
    {cycle: 0, positionCycle: 1.8, lane: 0, labelLane: 0, icon: chestIcon, label: 'Village start', status: 'confirmed'},
    {cycle: 2, lane: 1, labelLane: 1, icon: oakLogIcon, label: 'Wood acquired', status: 'confirmed'},
    {cycle: 3, lane: 0, labelLane: 2, icon: oakPlanksIcon, label: 'Planks/sticks', status: 'confirmed'},
    {cycle: 4, lane: 1, labelLane: 0, icon: craftingTableIcon, label: 'Table step', status: 'confirmed'},
    {cycle: 30, positionCycle: 29, lane: 0, labelLane: 0, icon: woodenPickaxeIcon, label: 'Pickaxe retained', status: 'confirmed'}
  ],
  'qwen-max': [
    {cycle: 0, positionCycle: 1.8, lane: 0, labelLane: 0, icon: chestIcon, label: 'Village start', status: 'confirmed'},
    {cycle: 2, lane: 1, labelLane: 1, icon: oakLogIcon, label: 'Wood acquired', status: 'confirmed'},
    {cycle: 3, lane: 0, labelLane: 2, icon: craftingTableIcon, label: 'Table step', status: 'confirmed'},
    {cycle: 18, lane: 0, labelLane: 0, icon: woodenPickaxeIcon, label: 'Pickaxe retained', status: 'confirmed'},
    {cycle: 30, positionCycle: 28, lane: 0, labelLane: 0, icon: cobblestoneIcon, label: '6 cobblestone', status: 'confirmed'},
    {cycle: 30, positionCycle: 30, lane: 1, labelLane: 2, label: 'Camera caveat', status: 'caveat'}
  ],
  'gpt54-mini': [
    {cycle: 0, positionCycle: 1.8, lane: 0, labelLane: 0, icon: chestIcon, label: 'Village start', status: 'confirmed'},
    {cycle: 6, lane: 0, labelLane: 1, icon: oakLogIcon, label: 'Wood acquired', status: 'confirmed'},
    {cycle: 12, lane: 0, labelLane: 0, icon: oakPlanksIcon, label: 'Materials crafted', status: 'confirmed'},
    {cycle: 30, positionCycle: 29, lane: 0, labelLane: 0, icon: craftingTableIcon, label: 'Table held', status: 'partial'}
  ],
  'gemini-31': [
    {cycle: 0, positionCycle: 1.8, lane: 0, labelLane: 0, icon: chestIcon, label: 'Village start', status: 'confirmed'},
    {cycle: 3, lane: 1, labelLane: 1, icon: oakLogIcon, label: 'Wood acquired', status: 'confirmed'},
    {cycle: 5, lane: 0, labelLane: 2, icon: craftingTableIcon, label: 'Table step', status: 'confirmed'},
    {cycle: 15, lane: 0, labelLane: 0, label: 'Recovery stalls', status: 'blocked'},
    {cycle: 30, positionCycle: 29, lane: 0, labelLane: 0, icon: stickIcon, label: 'Sticks/planks retained', status: 'partial'}
  ]
};

export const maxRequests = Math.max(...runs.map((run) => run.requests));
export const maxTokensPerObserved = Math.max(...runs.map((run) => run.tokensPerObserved));

export function compactNumber(value) {
  if (value >= 1000000) return `${(value / 1000000).toFixed(2)}M`;
  if (value >= 1000) return `${Math.round(value / 1000)}k`;
  return `${value}`;
}

export function OutcomeStack({run}) {
  const total = run.observed + run.blocked + run.noProgress;
  const segment = (value) => `${(value / total) * 100}%`;
  const percent = (value) => `${Math.round((value / total) * 100)}%`;
  return (
    <div className="outcomeBlock" aria-label={`${run.label} outcome distribution`}>
      <div className="outcomeStack" aria-hidden="true">
        <span className="segment observed" style={{width: segment(run.observed)}} />
        <span className="segment blocked" style={{width: segment(run.blocked)}} />
        <span className="segment noProgress" style={{width: segment(run.noProgress)}} />
      </div>
      <dl className="outcomeBreakdown">
        <div className="observed">
          <dt>Observed progress</dt>
          <dd>{run.observed}/30 <span>{percent(run.observed)}</span></dd>
        </div>
        <div className="blocked">
          <dt>Blocked</dt>
          <dd>{run.blocked}/30 <span>{percent(run.blocked)}</span></dd>
        </div>
        <div className="noProgress">
          <dt>No progress</dt>
          <dd>{run.noProgress}/30 <span>{percent(run.noProgress)}</span></dd>
        </div>
      </dl>
    </div>
  );
}

export function InventoryTokens({items}) {
  return (
    <ul className="inventoryTokens" aria-label="Key final inventory items">
      {items.map((item) => (
        <li key={`${item.label}-${item.qty}`}>
          <img src={item.icon} alt="" />
          <span>{item.label}</span>
          <b>x{item.qty}</b>
        </li>
      ))}
    </ul>
  );
}

export function ModelCard({run}) {
  return (
    <article className={`modelCard ${run.id}`}>
      <img src={run.image} alt={`${run.label} Minecraft report screenshot`} />
      <div className="modelBody">
        <p className="eyebrow">{run.provider}</p>
        <h3>{run.label}</h3>
        <p className="modelName">{run.model}</p>
        <p className="goalVerdict">{run.goalVerdict}</p>
        <p>{run.goalSummary}</p>
        <dl className="goalFacts">
          <div><dt>Highest goal state</dt><dd>{goalSteps[run.goalScore]?.label ?? 'Unknown'}</dd></div>
          <div><dt>Final material state</dt><dd>{run.finalGoalState}</dd></div>
          <div><dt>Blocker recovery</dt><dd>{run.blockerRead}</dd></div>
          <div><dt>Cost footprint</dt><dd>{run.requests} provider records, {compactNumber(run.tokens)} tokens</dd></div>
        </dl>
        <InventoryTokens items={run.inventoryItems} />
        <details className="diagnosticDetails">
          <summary>Cycle diagnostics</summary>
          <OutcomeStack run={run} />
        </details>
      </div>
    </article>
  );
}

export function BarRow({run, value, max, suffix = ''}) {
  return (
    <div className="barRow">
      <span className="barLabel">{run.label}</span>
      <span className="barTrack" aria-hidden="true">
        <span className={`barFill ${run.id}`} style={{width: `${(value / max) * 100}%`}} />
      </span>
      <span className="barValue">{compactNumber(value)}{suffix}</span>
    </div>
  );
}

export function MaterialProgressGraph({runs, eventsByRun}) {
  return (
    <figure className="materialGraph">
      <figcaption>
        <b>Work Point Maturity over time.</b> Icons mark durable material states, not repeated action attempts.
        Each icon sits above the timeline; the vertical guide drops to the cycle where that state was recorded.
      </figcaption>
      <div className="goalScale" aria-hidden="true">
        {goalSteps.map((step) => (
          <span key={step.level}>
            <img src={step.icon} alt="" />
            {step.label}
          </span>
        ))}
      </div>
      <div className="graphRows">
        {runs.map((run) => (
          <div className="graphRow" key={run.id}>
            <div className="graphRunLabel">
              <b>{run.label}</b>
              <span>{goalSteps[run.goalScore]?.label ?? 'Unknown'}</span>
            </div>
            <div className="graphLane">
              <div className="graphTrack" aria-label={`${run.label} material progress over cycles`}>
                <div className="graphTicks" aria-hidden="true">
                  {[10, 20, 30].map((cycle) => (
                    <span
                      className="graphTick"
                      key={`${run.id}-tick-${cycle}`}
                      style={{left: `${(cycle / 30) * 100}%`}}
                    >
                      C{cycle}
                    </span>
                  ))}
                </div>
                <div className="graphLine" />
                {(eventsByRun[run.id] ?? []).map((event, index) => (
                  <span
                    className={`graphEvent ${event.status}`}
                    key={`${run.id}-${event.label}-${index}`}
                    title={`C${String(event.cycle).padStart(2, '0')} ${event.label}`}
                    style={{
                      '--event-left': `${Math.min(97, Math.max(3, ((event.positionCycle ?? event.cycle) / 30) * 100))}%`,
                      '--event-level': event.lane ?? 0,
                      '--label-level': event.labelLane ?? event.lane ?? 0
                    }}
                  >
                    <span className="eventMarker">
                      {event.icon ? <img src={event.icon} alt="" /> : <b>!</b>}
                    </span>
                    <span className="eventStem" aria-hidden="true" />
                    <span className="eventLabel">
                      <b>C{String(event.cycle).padStart(2, '0')}</b>
                      <span>{event.label}</span>
                    </span>
                  </span>
                ))}
              </div>
            </div>
          </div>
        ))}
      </div>
      <div className="graphLegend">
        <span><b className="confirmedKey" />durable material state</span>
        <span><b className="partialKey" />partial or unresolved state</span>
        <span><b className="blockedKey" />blocked recovery</span>
        <span><b className="caveatKey">!</b>visual/log caveat</span>
      </div>
    </figure>
  );
}

<style>{`
.modelReport {
  --paper: #f8f2e5;
  --surface: #fffaf0;
  --ink: #1c2721;
  --muted: #58625a;
  --line: #d8cfbc;
  --accent: #2f6f55;
  --stone: #747a76;
  --blocked: #9c514a;
  --bar: #2d332f;
  --soft: #edf3e5;
  color: var(--ink);
}
.modelReport * { box-sizing: border-box; min-width: 0; }
.modelReport .eyebrow,
.modelReport .metaLabel {
  margin: 0 0 6px;
  color: var(--accent);
  font-size: 12px;
  font-weight: 800;
  letter-spacing: 0;
  text-transform: uppercase;
}
.modelReport .lead {
  color: var(--muted);
  font-size: 18px;
  line-height: 1.65;
  margin: 0 0 18px;
}
.reportHero {
  display: grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(280px, .8fr);
  gap: 18px;
  align-items: stretch;
  margin: 24px 0 30px;
  border: 1px solid var(--line);
  border-radius: 8px;
  background: var(--paper);
  overflow: hidden;
}
.reportHero img {
  width: 100%;
  height: 100%;
  min-height: 330px;
  object-fit: cover;
  display: block;
}
.heroCopy {
  padding: 26px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 14px;
}
.heroCopy h2 {
  margin: 0;
  font-size: 32px;
  line-height: 1.12;
}
.heroCopy p { margin: 0; color: var(--muted); line-height: 1.65; }
.heroFacts {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 10px;
}
.heroFacts span {
  display: block;
  padding: 10px;
  border: 1px solid var(--line);
  border-radius: 8px;
  background: rgba(255, 250, 240, .8);
  font-size: 13px;
}
.heroFacts b {
  display: block;
  color: var(--ink);
  font-size: 18px;
  font-variant-numeric: tabular-nums;
}
.metricGrid,
.modelGrid {
  display: grid;
  gap: 14px;
  grid-template-columns: repeat(auto-fit, minmax(min(100%, 250px), 1fr));
}
.metricCard,
.modelCard,
.noteBox,
.barPanel {
  border: 1px solid var(--line);
  border-radius: 8px;
  background: var(--surface);
}
.metricCard {
  padding: 16px;
}
.metricCard b {
  display: block;
  font-size: 28px;
  line-height: 1;
  font-variant-numeric: tabular-nums;
}
.metricCard span { color: var(--muted); font-size: 13px; }
.modelCard {
  overflow: hidden;
  display: flex;
  flex-direction: column;
}
.modelCard img {
  width: 100%;
  aspect-ratio: 16 / 9;
  object-fit: cover;
  display: block;
  border-bottom: 1px solid var(--line);
}
.modelBody {
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  flex: 1;
}
.modelBody h3 {
  margin: 0;
  font-size: 22px;
}
.modelName,
.inventory {
  color: var(--muted);
  font-size: 13px;
  line-height: 1.55;
  margin: 0;
  overflow-wrap: anywhere;
}
.modelBody p { margin: 0; line-height: 1.58; }
.goalVerdict {
  color: var(--ink);
  font-size: 15px;
  font-weight: 800;
}
.goalFacts {
  display: grid;
  gap: 8px;
  margin: 0;
}
.goalFacts div {
  padding: 9px 0;
  border-top: 1px solid var(--line);
}
.goalFacts dt {
  color: var(--muted);
  font-size: 11px;
  font-weight: 800;
  text-transform: uppercase;
}
.goalFacts dd {
  margin: 3px 0 0;
  color: var(--ink);
  font-size: 13px;
  line-height: 1.45;
}
.inventoryTokens {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
  margin: 0;
  padding: 0;
  list-style: none;
}
.inventoryTokens li {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  padding: 5px 7px;
  border: 1px solid var(--line);
  border-radius: 4px;
  background: #fbf5e7;
  color: var(--ink);
  font-size: 12px;
}
.inventoryTokens img {
  width: 18px;
  height: 18px;
  image-rendering: pixelated;
  object-fit: contain;
}
.inventoryTokens b {
  font-variant-numeric: tabular-nums;
}
.diagnosticDetails {
  border-top: 1px solid var(--line);
  padding-top: 8px;
}
.diagnosticDetails summary {
  cursor: pointer;
  color: var(--muted);
  font-size: 12px;
  font-weight: 800;
  text-transform: uppercase;
}
.outcomeBlock {
  display: grid;
  gap: 8px;
}
.outcomeStack {
  display: flex;
  width: 100%;
  height: 10px;
  border-radius: 999px;
  overflow: hidden;
  border: 1px solid var(--line);
  background: #ebe3d2;
}
.segment.observed { background: var(--accent); }
.segment.blocked { background: var(--blocked); }
.segment.noProgress { background: var(--stone); }
.outcomeBreakdown {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(92px, 1fr));
  gap: 6px;
  margin: 0;
}
.outcomeBreakdown div {
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 8px;
  background: #fbf5e7;
}
.outcomeBreakdown dt {
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--muted);
  font-size: 10px;
  font-weight: 800;
  line-height: 1.2;
  text-transform: uppercase;
}
.outcomeBreakdown dt::before {
  content: "";
  width: 8px;
  height: 8px;
  border-radius: 999px;
  flex: 0 0 auto;
}
.outcomeBreakdown .observed dt::before { background: var(--accent); }
.outcomeBreakdown .blocked dt::before { background: var(--blocked); }
.outcomeBreakdown .noProgress dt::before { background: var(--stone); }
.outcomeBreakdown dd {
  margin: 4px 0 0;
  color: var(--ink);
  font-size: 13px;
  font-weight: 800;
  font-variant-numeric: tabular-nums;
}
.outcomeBreakdown dd span {
  color: var(--muted);
  font-size: 11px;
  font-weight: 700;
}
.miniStats {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 8px;
  margin: 0;
}
.miniStats div {
  padding: 8px;
  border: 1px solid var(--line);
  border-radius: 8px;
  background: var(--soft);
}
.miniStats dt {
  color: var(--muted);
  font-size: 11px;
  text-transform: uppercase;
  font-weight: 800;
}
.miniStats dd {
  margin: 2px 0 0;
  font-variant-numeric: tabular-nums;
  font-weight: 800;
}
.legend {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin: 12px 0 0;
  color: var(--muted);
  font-size: 13px;
}
.legend b {
  color: var(--ink);
}
.legend span::before {
  content: "";
  display: inline-block;
  width: 10px;
  height: 10px;
  margin-right: 6px;
  border-radius: 3px;
  vertical-align: -1px;
}
.legend .observedKey::before { background: var(--accent); }
.legend .blockedKey::before { background: var(--blocked); }
.legend .noProgressKey::before { background: var(--stone); }
.barPanel {
  padding: 16px;
  margin: 16px 0;
}
.barPanel h3 { margin: 0 0 12px; }
.barRow {
  display: grid;
  grid-template-columns: minmax(130px, .75fr) minmax(120px, 2fr) minmax(70px, .35fr);
  gap: 10px;
  align-items: center;
  margin: 10px 0;
}
.barLabel,
.barValue {
  font-size: 13px;
  font-weight: 750;
}
.barValue {
  text-align: right;
  font-variant-numeric: tabular-nums;
}
.barTrack {
  height: 12px;
  border-radius: 6px;
  background: #eee4d1;
  border: 1px solid var(--line);
  overflow: hidden;
}
.barFill {
  display: block;
  height: 100%;
  background: var(--bar);
}
.materialGraph {
  margin: 18px 0 24px;
  padding: 16px;
  border: 1px solid var(--line);
  border-radius: 8px;
  background: #fbf5e7;
}
.materialGraph figcaption {
  margin: 0 0 14px;
  color: var(--muted);
  line-height: 1.55;
}
.materialGraph figcaption b {
  color: var(--ink);
}
.goalScale {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 14px;
}
.goalScale span {
  display: inline-flex;
  align-items: center;
  gap: 5px;
  color: var(--muted);
  font-size: 12px;
}
.goalScale img {
  width: 18px;
  height: 18px;
  image-rendering: pixelated;
}
.graphRows {
  display: grid;
  gap: 16px;
  margin-top: 8px;
}
.graphRow {
  display: grid;
  grid-template-columns: 108px minmax(0, 1fr);
  gap: 8px;
  align-items: start;
}
.graphRunLabel b {
  display: block;
  color: var(--ink);
  font-size: 14px;
  line-height: 1.2;
}
.graphRunLabel span {
  color: var(--muted);
  display: block;
  font-size: 11px;
  line-height: 1.25;
  margin-top: 4px;
}
.graphLane {
  overflow-x: auto;
  padding-bottom: 3px;
}
.graphTrack {
  --axis-top: 92px;
  --label-top: 112px;
  position: relative;
  min-width: 760px;
  min-height: 206px;
  border-radius: 6px;
  background:
    linear-gradient(to right, transparent calc(33.333% - 1px), rgba(45, 51, 47, .12) calc(33.333% - 1px), rgba(45, 51, 47, .12) 33.333%, transparent 33.333%) 0 0 / 33.333% 100%,
    #fffaf0;
  border: 1px solid var(--line);
  overflow: hidden;
}
.graphTicks {
  position: absolute;
  inset: 0 18px;
  pointer-events: none;
}
.graphTick {
  position: absolute;
  top: calc(var(--axis-top) + 7px);
  transform: translateX(-50%);
  color: var(--muted);
  font-size: 11px;
  font-weight: 850;
  font-variant-numeric: tabular-nums;
  line-height: 1;
  text-transform: uppercase;
  white-space: nowrap;
}
.graphTick::before {
  content: "";
  position: absolute;
  left: 50%;
  top: -12px;
  width: 1px;
  height: 10px;
  background: #bdb19d;
}
.graphTick:last-child {
  transform: translateX(-100%);
}
.graphTick:last-child::before {
  left: auto;
  right: 0;
}
.graphLine {
  position: absolute;
  left: 18px;
  right: 18px;
  top: var(--axis-top);
  height: 2px;
  background: #d1c5af;
}
.graphEvent {
  position: absolute;
  left: var(--event-left);
  top: 0;
  width: 96px;
  height: 190px;
  transform: translateX(-50%);
  z-index: 1;
}
.eventMarker {
  position: absolute;
  left: 50%;
  top: calc(10px + (var(--event-level) * 34px));
  transform: translateX(-50%);
  display: grid;
  place-items: center;
  width: 44px;
  height: 44px;
  border: 2px solid var(--accent);
  border-radius: 6px;
  background: #fffaf0;
  box-shadow: 0 1px 0 rgba(28, 39, 33, .08);
}
.eventMarker img {
  width: 34px;
  height: 34px;
  image-rendering: pixelated;
  object-fit: contain;
}
.eventStem {
  position: absolute;
  left: 50%;
  top: calc(54px + (var(--event-level) * 34px));
  width: 1px;
  height: calc(38px - (var(--event-level) * 34px));
  background: #8d9b8f;
  transform: translateX(-50%);
}
.eventStem::after {
  content: "";
  position: absolute;
  left: 50%;
  bottom: -4px;
  width: 7px;
  height: 7px;
  border-radius: 999px;
  background: var(--accent);
  transform: translateX(-50%);
}
.eventLabel {
  position: absolute;
  left: 50%;
  top: calc(var(--label-top) + (var(--label-level) * 28px));
  width: 92px;
  transform: translateX(-50%);
  color: var(--muted);
  font-size: 11px;
  font-variant-numeric: tabular-nums;
  line-height: 1.12;
  text-align: center;
}
.eventLabel b {
  display: block;
  color: var(--ink);
  font-size: 11px;
  letter-spacing: 0;
}
.eventLabel span {
  display: block;
  overflow-wrap: anywhere;
}
.graphEvent.partial .eventMarker {
  border-style: dashed;
  border-color: var(--stone);
}
.graphEvent.partial .eventStem { background: var(--stone); }
.graphEvent.partial .eventStem::after { background: var(--stone); }
.graphEvent.blocked .eventMarker {
  border-color: var(--blocked);
  color: var(--blocked);
}
.graphEvent.blocked .eventStem { background: var(--blocked); }
.graphEvent.blocked .eventStem::after { background: var(--blocked); }
.graphEvent.blocked .eventLabel b,
.graphEvent.blocked .eventLabel span {
  color: var(--blocked);
}
.graphEvent.caveat .eventMarker {
  border-color: #a8742c;
  color: #7b5520;
}
.graphEvent.caveat .eventStem { background: #a8742c; }
.graphEvent.caveat .eventStem::after { background: #a8742c; }
.graphEvent.caveat .eventLabel b,
.graphEvent.caveat .eventLabel span {
  color: #7b5520;
}
.graphLegend {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 12px;
  color: var(--muted);
  font-size: 12px;
}
.graphLegend span {
  display: inline-flex;
  align-items: center;
  gap: 6px;
}
.graphLegend b {
  display: inline-grid;
  place-items: center;
  width: 12px;
  height: 12px;
  border-radius: 3px;
  border: 2px solid var(--accent);
  color: #7b5520;
  font-size: 10px;
  line-height: 1;
}
.graphLegend .partialKey { border-style: dashed; border-color: var(--stone); }
.graphLegend .blockedKey { border-color: var(--blocked); }
.graphLegend .caveatKey { border-color: #a8742c; }
.noteBox {
  padding: 16px;
  margin: 18px 0;
  background: #fbf5e7;
}
.noteBox strong { color: var(--ink); }
.checklist {
  display: grid;
  gap: 8px;
  grid-template-columns: repeat(auto-fit, minmax(min(100%, 180px), 1fr));
  margin: 12px 0 0;
}
.checklist span {
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 10px;
  background: var(--soft);
  font-size: 13px;
}
.checklist b { display: block; }
@media (max-width: 820px) {
  .reportHero { grid-template-columns: 1fr; }
  .reportHero img { min-height: 220px; }
  .heroCopy { padding: 18px; }
  .heroCopy h2 { font-size: 26px; }
  .barRow { grid-template-columns: 1fr; gap: 6px; }
  .barValue { text-align: left; }
  .miniStats { grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .graphRow { grid-template-columns: 1fr; }
  .graphTrack { min-width: 680px; }
}
`}</style>

<div className="modelReport">

This write-up describes the project state completed on June 30, 2026.

The larger research question is still open. The project has not yet picked a final
claim such as action-consequence prediction, social behavior, or model comparison.
The current work is more basic: make a natural Minecraft village setup where those
questions can later be tested without relying on a staged toy world.

For this check, I ran four provider-backed model lanes through the same small
Minecraft LLM-agent harness: two ModelScope lanes using Qwen Plus and Qwen Max,
plus OpenAI and Gemini reference lanes.

Outside ModelScope, I stayed within available free-tier or low-quota access. I
did not run frontier paid comparisons such as GPT-5.5 or Claude Opus 4.8 for this
post, so the results should be read as a harness check, not a frontier-model
ranking.

This is not a leaderboard, not a broad model claim, and not a decision about
whether interaction history can beat an LLM prior. The point was narrower: check
whether the current environment can support concrete Minecraft work, repeated
screenshots, inventory review, and blocker analysis across multiple model lanes.

<section className="reportHero" aria-label="Natural village report summary">
  <img src={heroVillage} alt="Minecraft natural village start position with the agent near houses and a path" />
  <div className="heroCopy">
    <p className="eyebrow">Minecraft natural village - environment check</p>
    <h2>One practical Minecraft goal, four attempts to reach it.</h2>
    <p>
      Each lane started from the same natural village scenario. The actor was asked to
      create a useful village-adjacent work point: collect wood, craft basic materials,
      make or use a crafting table, recover from movement blockers, and continue toward
      simple material work instead of only describing a plan.
    </p>
    <div className="heroFacts">
      <span><b>120</b> completed cycles</span>
      <span><b>384</b> visual captures</span>
      <span><b>5.05M</b> total tokens</span>
      <span><b>1</b> stone follow-through</span>
    </div>
  </div>
</section>

</div>

<!--truncate-->

<div className="modelReport">

## Experiment Setup

The run used `natural-village-spawn-v1` with seed `4167799982467607063`. Each model
received a fresh world lane, 30 cycles, the report visual profile, and Minecraft
server version `1.21.4` for `prismarine-viewer` screenshots.

The actor-facing objective was intentionally practical:

```text
From a fresh natural village-adjacent start, establish a useful work point for
continued material work. Collect wood, craft basic materials, make or use a
crafting table, recover from blockers, and leave the world in a state that can
be reviewed from inventory, screenshots, and runtime records.
```

This is why the run looks like basic Minecraft work instead of an explicit
social-simulation demo. The aim was to check whether the environment can support
later research questions, not to declare the research target solved.

<div className="metricGrid" aria-label="Experiment setup metrics">
  <div className="metricCard">
    <p className="metaLabel">world</p>
    <b>natural village</b>
    <span>same seed, fresh world per lane</span>
  </div>
  <div className="metricCard">
    <p className="metaLabel">providers</p>
    <b>3 APIs</b>
    <span>ModelScope, OpenAI API, Gemini API</span>
  </div>
  <div className="metricCard">
    <p className="metaLabel">runtime</p>
    <b>action loop</b>
    <span>model output -> action -> world records</span>
  </div>
  <div className="metricCard">
    <p className="metaLabel">visuals</p>
    <b>3 cameras</b>
    <span>first-person, follow, high view every cycle</span>
  </div>
</div>

Screenshots are included because they make the run easier to inspect. They are not
used by themselves to decide block identity or material progress. For those claims,
I checked inventory, observe/world-state scans, runtime status, and action records.

## Practical Goal State Over Time

<MaterialProgressGraph runs={runs} eventsByRun={materialEvents} />

The graph does not reward repeated actions. It only moves when the actor leaves
behind a more useful material state: wood, basic materials, a table step, a retained
tool, or stone follow-through. That is closer to the question this run actually
asks: did the NPC autonomously leave the village-adjacent world in a more useful
state than it found it?

## Model Cards

<div className="modelGrid">
  {runs.map((run) => <ModelCard key={run.id} run={run} />)}
</div>

<div className="legend" aria-label="Outcome color legend">
  <b>Outcome colors used in every model card:</b>
  <span className="observedKey">observed progress</span>
  <span className="blockedKey">blocked</span>
  <span className="noProgressKey">no progress</span>
</div>

## Did The NPC Reach The Practical Goal?

The useful result is not that one provider "won" a benchmark. The practical question
is whether the actor made the natural village more workable for continued material
work.

- Qwen Max reached the deepest final state: table step, retained wooden pickaxe,
  and 6 cobblestone.
- Qwen Plus reached a stable wood-level work point with a retained wooden pickaxe,
  but did not continue into stone.
- GPT-5.4 mini gathered useful materials and held a crafting table, but did not
  settle the work point.
- Gemini 3.1 Flash Lite reached table-level progress, then spent much of the run
  stuck around recovery.

Provider usage still matters, but only as a cost caveat. It should not be read as
the research outcome:

| Lane | Highest practical goal state | Provider records | Total tokens |
| --- | --- | ---: | ---: |
| Qwen Plus | Tool retained | 38 | 1.13M |
| Qwen Max | Stone follow-through | 38 | 1.09M |
| GPT-5.4 mini | Crafting table held/unresolved | 204 | 1.12M |
| Gemini 3.1 Flash Lite | Crafting table step, recovery stalled | 67 | 1.71M |

## What The NPC Actually Managed To Do

The useful result is modest: the harness can now produce model-driven material work
in a natural village without a hand-staged toy arena. The records show logs becoming
planks and sticks, table progress appearing, tools showing up in inventory, and one
lane reaching cobblestone.

That does not make this a general statement about Minecraft competence or about a
provider family. It only says the environment is now useful enough to design sharper
natural-village runs where material behavior is the input to a harder question,
not the claimed research result.

<div className="noteBox">
  <strong>Important boundary:</strong> this run mostly evaluates one-actor material
  work continuity in a natural village. It does not prove social emergence, long-run
  settlement behavior, or a learnable action-consequence target beyond LLM prior.
</div>

## How I Read The Evidence

The model cards show screenshots because they help humans inspect the run. I did
not use pixels alone to decide progress. Material claims in this post come from
inventory, observe/world-state scans, runtime status, and action records.

That matters most for the Qwen Max lane: the final third-person camera produced a
known terrain cross-section artifact, so I use the cleaner cycle-20 image for visual
context while treating the stone result as an inventory and world-state claim. The
same rule applies to every lane: screenshots are context, not ground truth.

## What Still Failed

The failures are more useful than another broad model comparison. They point to the
next experiment design:

- movement recovery can dominate after early crafting progress;
- table crafting and recipe handling still create friction;
- generated action-skill candidates can be rejected before a Minecraft action executes;
- the objective mixed workbench, navigation recovery, material continuity, and
  longer follow-through into one loose task.

For the next version of this experiment, I would make the target sharper:

```text
Place a crafting table and maintain a village-adjacent work point with bounded
material follow-through.
```

That would still produce useful material records without mixing workbench, shelter,
storage, navigation recovery, and long-horizon continuity into one run.

## What This Does Not Prove

<div className="checklist">
  <span><b>Not a leaderboard</b> Four completed lanes, one seed, one objective, one harness.</span>
  <span><b>Not a prediction-prior decision</b> No prediction-arm comparison was run here.</span>
  <span><b>Visuals are for review</b> Screenshots help humans inspect behavior; block identity comes from logs, inventory, and world-state scans.</span>
  <span><b>Visual caveat</b> Qwen Max cycle 30 third-person follow/high views show a camera cross-section artifact.</span>
  <span><b>Gemini pacing caveat</b> The completed lane used `GEMINI_MIN_REQUEST_INTERVAL_MS=15000`.</span>
  <span><b>Excluded lanes</b> Gemini 2.5 Flash Lite, Gemma 4 31B, and unpaced Gemini 3.1 were not comparable completed lanes.</span>
</div>

## Run Notes

This article is based on the repository state completed on June 30, 2026. The repo
keeps the full HTML report, combined model-comparison data, per-lane summaries,
provider preflight notes, run reports, and screenshot folders. I am not listing raw
internal file paths here because the public post should explain the result rather
than make readers chase local repository paths.

The main lesson is modest: the environment is now close enough to run small natural
village material-work comparisons, but the research target is still being narrowed.
The next runs should use sharper tasks before making claims about action-consequence
learning or social behavior.

</div>
