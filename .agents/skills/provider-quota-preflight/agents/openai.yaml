interface:
  display_name: "Provider Quota Preflight"
  short_description: "Check provider quota before live runs."
  default_prompt: "Use $provider-quota-preflight to check quota before a provider-backed run."
