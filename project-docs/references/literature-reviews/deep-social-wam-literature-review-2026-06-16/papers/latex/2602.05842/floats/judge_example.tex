\begin{table}[t!]
    \caption{Example prompt for LLM-as-a-judge in our ablation studies. In general, we find LLM-as-a-judge is unreliable and can award high scores to predictions that are not meaningful. ``predicted\_next\_state'' are contents extracted from the \texttt{<next\_state>}\texttt{</next\_state>} tags in the generated response.}
    \label{tab:alfworld_judge_example}
    \begin{tabular}{p{16.5cm}}
    \toprule
    You are a helpful judge AI agent.

    \# Current state
    \newline
    \textcolor{blue}{\{}\textcolor{blue}{current\_state}\textcolor{blue}{\}}
    \newline

    \# Action proposed by an AI agent
    \newline
    \textcolor{blue}{\{}\textcolor{blue}{action\_text}\textcolor{blue}{\}}
    \newline

    \# Predicted next observation
    \newline
    After executing the above action, another AI agent predicted the next observation as follows:
    \newline
    \textcolor{blue}{\{}\textcolor{blue}{predicted\_next\_state}\textcolor{blue}{\}}
    \newline

    \# Actual next observation
    \newline
    The actual next observation from the environment is as follows:
    \newline
    \textcolor{blue}{\{}\textcolor{blue}{actual\_next\_state}\textcolor{blue}{\}}
    \newline

    \# Evaluate the predicted next observation
    \newline
    Now, your task is to evaluate how well the predicted next observation matches the actual next observation.
    Specifically, you need to judge whether the prediction demonstrates a \emph{genuine} understanding of the environment dynamics relevant to the task, using the actual next observation as reference.
    \newline

    - If all important task-related information in the actual next observation is present in the predicted next observation, assign a score of \texttt{1.0}.
    \newline
    - Otherwise, assign a score of \texttt{0.0}.
    \newline

    \# Your output format
    \newline
    Your task is to output a JSON object in the following format:
    \newline
    \texttt{<json>}
    \newline
    \texttt{\{}
    \newline
    \texttt{\ \ "analysis": "which important task-related content is present/missing in the predicted next observation.",}
    \newline
    \texttt{\ \ "score": 0.0 or 1.0}
    \newline
    \texttt{\}}
    \newline
    \texttt{</json>}

    Directly output the JSON object. DO NOT generate anything else.
    \\
    \midrule
    \emph{Example generation that achieved a score of 1.0:}

    \texttt{<think>} The potential action is to go to countertop 1. After this action, I should observe that I am at countertop 1, and the contents of the countertop will be displayed. The task is not yet completed as I have not found the tomato or started the process of putting it in the microwave. \texttt{</think>}
    
    \texttt{<next\_state>} The immediate next observation after going to countertop 1 will describe the contents of countertop 1 or indicate if there is anything special about the countertop.\texttt{</next\_state>}
    \\
    \midrule
    \emph{Example actual next state:}

    
    You arrive at countertop 1. On the countertop 1, you see a apple 2, a bread 2, (...omitted), a knife 4, a knife 1, a potato 1, a saltshaker 1, a spatula 1, a spoon 2, and a spoon 1. \\
    \bottomrule
    \end{tabular}
\end{table}
