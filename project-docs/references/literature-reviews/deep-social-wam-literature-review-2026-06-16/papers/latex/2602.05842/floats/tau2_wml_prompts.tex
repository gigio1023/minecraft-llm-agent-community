\begin{table}[t!]
    \caption{Prompt for \wmrlshort{} in $\tau^{2}$ Bench, given $s_{\le t}$ and an action $a_t$. Final output within the \texttt{<next\_state>} \texttt{</next\_state>} tags is used to compare with the next state in $r^{\textrm{WM}}(\hat{s}_{t+1}, s_{t+1})$ during RL.}
    \label{tab:tau2_wmrl_prompt}
    \begin{tabular}{p{16.5cm}}
    \toprule
    You are a customer service agent that helps the user according to the <policy> provided below...\emph{\textcolor{gray}{// omitting task instruction, policy, and tool usage details here for brevity}}
    \newline

    \# User Information

    The following information about the user is available:

    % \textcolor{blue}{\{}\textcolor{blue}{current\_state}\textcolor{blue}{\}}
    \textcolor{blue}{\{}\textcolor{blue}{available\_user\_info}\textcolor{blue}{\}}
    \newline

    \# History

    \textcolor{blue}{\{}\textcolor{blue}{current\_state}\textcolor{blue}{\}}


    \textbf{Potential assistant response:}
    \textcolor{blue}{\{}\textcolor{blue}{action}\textcolor{blue}{\}}
    \newline


    \# Your Task

    Now, your task is to predict the immediate next user/tool response if the above `potential assistant response' is used based on the available information above.

    % Once you've finished your reasoning, you should describe the next user/tool response (use the past and current user/tool responses as examples!) and present them within \texttt{<next\_state>} \texttt{</next\_state>} tags.
    Once you've finished your thinking, format your final prediction of the next user/tool response and task completion status within \texttt{<next\_state>} \texttt{</next\_state>} tags. Note that a user response should be written as plain text. A tool response may be a short status message (e.g., no data found, error, transaction success, etc.,) or a JSON object; if it is JSON, only predict the JSON schema in OpenAPI format rather than actual values (e.g., \{"type": "object", "properties": \{"customer\_id": \{"type": "string"\}, "full\_name": \{"type": "string"\}\},...\}).
    \\
    \bottomrule
    \end{tabular}
\end{table}


\begin{table}[t!]
    \caption{Prompt for \wmsftshort{} in $\tau^{2}$ Bench, given $s_{\le t}$ and an action $a_t$. The label is the next state $s_{t+1}$.}
    \label{tab:tau2_wmsft_prompt}
    \begin{tabular}{p{16.5cm}}
    \toprule
    You are a customer service agent that helps the user according to the <policy> provided below...\emph{\textcolor{gray}{// omitting task instruction, policy, and tool usage details here for brevity}}
    \newline

    \# User Information

    The following information about the user is available:

    % \textcolor{blue}{\{}\textcolor{blue}{current\_state}\textcolor{blue}{\}}
    \textcolor{blue}{\{}\textcolor{blue}{available\_user\_info}\textcolor{blue}{\}}
    \newline

    \# History

    \textcolor{blue}{\{}\textcolor{blue}{current\_state}\textcolor{blue}{\}}


    \textbf{Potential assistant response:}
    \textcolor{blue}{\{}\textcolor{blue}{action}\textcolor{blue}{\}}
    \newline


    \# Your Task

    Now, your task is to predict the immediate next user/tool response if the above `potential assistant response' is used based on the available information above.

    DO NOT perform any thinking. Directly present your final prediction of the next user/tool response within \texttt{<next\_state>} \texttt{</next\_state>} tags. Note that a user response should be written as plain text. A tool response may be a short status message (e.g., no data found, error, transaction success, etc.,) or a JSON object; if it is JSON, only predict the JSON schema in OpenAPI format rather than actual values (e.g., \{"type": "object", "properties": \{"customer\_id": \{"type": "string"\}, "full\_name": \{"type": "string"\}\},...\}).
    \\
    \midrule
    Label: \texttt{<think>} \texttt{</think>}

    \texttt{<next\_state>} \textcolor{blue}{\{}\textcolor{blue}{next\_state}\textcolor{blue}{\}} \texttt{</next\_state>}
    \\
    \bottomrule
    \end{tabular}
\end{table}