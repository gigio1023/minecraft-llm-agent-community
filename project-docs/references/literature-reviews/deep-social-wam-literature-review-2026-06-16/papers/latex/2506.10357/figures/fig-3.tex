
\begin{figure*}[htbp]
    \centering
    \includegraphics[width=1\textwidth]{figures/fig3.pdf}
    \caption{Different agent framework in Minecraft. \textbf{(A)} Goal-conditioned policy which based on Transformer-XL architecture. \textbf{(B)} Function calling, which employs LLM to generate executable functions. \textbf{(C)} (M)LLM as the high-level planner, which then employs a goal-conditioned policy to generate low-level actions. \textbf{(D)} MLLM generates latent tokens that serve as conditioning inputs for the policy. \textbf{(E)} End-to-end MoE architecture (Ours) which endowed with multi-dimensional capabilities.}
    \label{fig:fig3}
\end{figure*}


