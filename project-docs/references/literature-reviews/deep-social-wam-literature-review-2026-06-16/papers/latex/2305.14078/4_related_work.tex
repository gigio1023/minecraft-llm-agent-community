\section{Related work}\label{sec:related_work}


% \textbf{Monte Carlo Tree Search.} Monte Carlo Tree Search (MCTS) \cite{kocsis2006bandit, coulom2007efficient, browne2012survey} is a scalable online planning algorithm for sequential decision-making. It uses random sampling in the tree search to tackle intractable large-scale problems. To trade-off between exploration and exploitation, UCT \cite{kocsis2006bandit} applied UCB to select actions for simulation.
% % % \begin{equation}
% % $    \argmax_{a\in A}Q(h_t, a) + c_\textrm{uct}\sqrt{\frac{\log N(h_t)}{N(h_t,a)}},$
% % % \end{equation}
% % where $h_t$ is the history of observations and actions that determines a node in a tree, $Q$ is the average received value of taking action $a$ at $h_t$, $N(h_t)$ is the number of times the node $h_t$ has been visited, and $N(h_t, a)$ is the number of times action $a$ has been selected at node $h_t$ in the search so far. 
% This method requires each action to be explored at least once, requiring massive sampling. Silver et al. \cite{silver2017mastering, silver2018general} created PUCT that applies learned policy prior in PUCB \cite{rosin2011multi} to bias the action selection.
% % % \begin{equation}
% % $    \argmax_{a\in A} Q(h_t, a) + c_\textrm{puct} \pi(a|h_t)\frac{\sqrt{N(h_t)}}{N(h_t, a) + 1}.$
% % % \end{equation}
% % % The policy $\pi(a|h_t)$ is trained using behavior cloning of the action at the root of the search tree
% % from previous execution results in tree search. 
% The method has been successfully applied in long-horizon planning games such as Go \cite{silver2017mastering, silver2018general}. However, task-specific training makes this approach difficult to apply to unseen tasks.
% In this work, we use the PUCT as the algorithm framework for planning to enable the exploitation of the commonsense knowledge of the world encoded in LLMs. 

% research field: planning
Task planning has a long-standing history in the AI research community. In the early stage, many studies \cite{aeronautiques1998pddl, hoffmann2001ff, helmert2006fast, garrett2020pddlstream} focused on task planning in a discrete state space with deterministic transitions. These methods are intractable for large-scale, long-horizon problems. Recently, researchers have used learning-based methods to learn planning policies directly \cite{li2022pre} or to learn search heuristics to accelerate planning \cite{silver2017mastering, silver2018general, xu2019regression, cai2019lets, sharma-etal-2022-skill}. Those policies or heuristics are not generalizable to other unseen settings. Most recently, the pre-trained LLMs have been applied as few-shot policies for task planning \cite{huang2022language, ahn2022can, huang2022inner}. However, the planning policy may not have good compositional generalizablity. In the robotics community, many studies proposed that task planning should be integrated with physical-level motion planning, i.e., task and motion planning (TAMP) \cite{garrett2021integrated, garrett2020pddlstream, ding2023task}. This paper focuses on large-scale task planning with partial observations and large, object-centric domains, in which classical planning is intractable and learning-based methods require massive data.  

Various approaches are proposed to scale up to large-scale planning problems. Initially, the Monte Carlo method \cite{kocsis2006bandit, coulom2007efficient, browne2012survey} is proposed to tackle intractable large-scale problems using random sampling in the tree search. However, further scaling up to the problem with a large domain with sparse rewards requires massive sampling. Silver et al. \cite{silver2017mastering, silver2018general} integrate MCTS with deep learning to bias the action selection and reduce sampling. The idea has been successfully applied in large-scale planning scenarios \cite{silver2017mastering, silver2018general, cai2019lets, schrittwieser2020mastering}. Most recently, studies show that LLM can be a few-shot policy for task planning \cite{huang2022language, ahn2022can, huang2022inner} in the large domain, while it may suffer from hallucinations. In this paper, we show that LLMs' commonsense knowledge can guide a search algorithm, reducing the search space sufficiently for practical planning and producing reasoned results.

% novelty: using llm as a model or policy
How to use LLMs for a planning/reasoning problem? LLMs have been used as a few-shot policy for language-conditioned task planning \cite{huang2022language, ahn2022can, huang2022inner}, or a policy for multi-step reasoning problems \cite{wei2022chain, yao2023tree}. However, recent research \cite{dziri2023faith} also suggests that transformer LLMs are inherently limited for solving multi-step reasoning problems. On the other hand, similar to our work, some studies try to use LLMs as heuristics \cite{zhang2023planning} or transition function \cite{hao2023reasoning} in MCTS, boosting the performance in coding or small-scale reasoning. However, the literature has not discussed utilizing LLMs as a world model in depth. This paper shows the benefits of using LLM to model the world, as well as using description length analysis to decide how to use LLM for planning/reasoning problems.

% Our main experiment uses object rearrangement tasks \cite{batra2020rearrangement} in household environments to demonstrate the method. Object rearrangement is a typical and comprehensive problem with large-scale, long-horizon task planning. It has many practical implications in everyday life \cite{batra2020rearrangement, szot2021habitat, weihs2021visual, kant2022housekeep, huang2019large}, such as setting the table, tidying up the room, loading the dishwasher, and more. The comprehensiveness comes from its coverage of many instruction-following subtasks, such as object searching, navigation, and object pick-and-place. It also requires long-horizon planning to determine how to pick an object, navigate, and place it somewhere. Some prior works \cite{ahn2022can, zhao2022differentiable, gu2022multi, szot2021habitat} focus on grounding low-level robot actions in object rearrangement tasks, therefore using a limited number of objects or a small domain for simplicity. The resulting planning tasks are simpler. We select the large-scale house environment for experiments, where the scale of task planning is significantly more complex. 


% research question: planning with large-scale domain
% model-based planning, learning-based planning
% The execution of natural language instructions entails long-horizon task planning due to the inherent abstraction of the language.
% % To attain the desired outcome, accomplishing the goal specified in the instructions necessitates the execution of multiple steps of action in a particular order. 
% For task planning, early attempts \cite{aeronautiques1998pddl, hoffmann2001ff, helmert2006fast, garrett2020pddlstream} used symbolic representation and search algorithms to efficiently carry out small-scale or short-horizon problems; these methods do not work well for large-scale, long-horizon problems. Recently, researchers \cite{silver2017mastering, silver2018general, xu2019regression, cai2019lets, sharma-etal-2022-skill} have used deep learning and reinforcement learning methods to learn search heuristics to accelerate planning. Those learned policies or heuristics are not generalizable to other unseen settings. Most recently, the pre-trained LLMs have been exhibiting impressive ability for format following and content generation, allowing them to be applied as policies for task planning \cite{huang2022language, ahn2022can, huang2022inner}. However, the planning policy may suffer from hallucination issues of LLMs. Our method uses the LLM's knowledge of the world together with a search algorithm for better-reasoned decision-making. To operate in the real world, task planning should be integrated with physical-level motion planning, i.e., task and motion planning (TAMP) \cite{garrett2021integrated, garrett2020pddlstream}. Limited by the scope, we consider the influence of physical-level motion planning as future work.

% novelty: llm as commonsense world model


% novelty
% Heuristics are widely used in planning, but commonsense knowledge is challenging to apply. The UCT and PUCT heuristics were applied in Monte Carlo Tree Search to trade off the exploration and exploitation in planning. It further developed model-based Reinforcement Learning to solve searching in MDP problems, which learns the transition dynamics of the world and plans concerning the learned model. After that, deep learning techniques boost the development of model-free reinforcement learning, which directly learns the planning policy and value function prediction from interacting with the environment. Recently, AlphaGo and AlphaZero used learned prior policy and predicted value function in the PUCT heuristics to guide the Monte Carlo Tree Search, which has achieved significant success in long-horizon planning games like Go. In addition, the learned heuristics are also used in long-horizon task-planning settings. However, those learned heuristics are task-specific and hard to generalize outside the dataset. Other studies use symbolic knowledge bases to construct commonsense that help ground robot plans. However, due to being handcrafted, knowledge bases are difficult to cover a large amount of commonsense knowledge. One similar research work also explored using language models as heuristics for Monte Carlo Tree Search in code generation. In contrast, their utility is task-specific and cannot be generalized to other planning problems. This paper examines the promising usage of large language models as universal commonsense heuristics for common task-planning problems in household settings.

%%% Local Variables:
%%% TeX-master: "./main"
%%% End: