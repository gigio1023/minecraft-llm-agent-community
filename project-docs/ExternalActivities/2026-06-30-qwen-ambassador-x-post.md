# X Post Draft: Minecraft Agent Natural-Village Evaluation

Purpose: one public X post that points to the repo/report without overstating
the result.

Compliance stance: use personal/open-source language. Do not mention employer or
internal work. Keep the headline on the Minecraft harness and evidence; mention
ModelScope/Qwen only as the concrete model-provider example used in this run.

## Single Post

```text
I ran a Minecraft LLM-agent harness test: 30 cycles near the same natural village, with runtime checks and screenshots.

Two lanes used ModelScope Qwen Plus/Max. The useful part is the artifact trail: material progress, blockers, and camera caveats.

Report: <PUBLIC_DOCUSARUS_RICH_VISUAL_REPORT_URL>
```

Character count without URL placeholder: approximately 249.

## Slightly More Technical Variant

```text
Small Minecraft agent experiment: four 30-cycle provider lanes on the same natural-village task.

I used ModelScope Qwen Plus/Max for two lanes, then compared the artifact trail with OpenAI + Gemini reference lanes.

<PUBLIC_DOCUSARUS_RICH_VISUAL_REPORT_URL>
```

Character count without URL placeholder: approximately 262.

## Thread Variant

Post 1:

```text
I ran a small Minecraft-agent village experiment:

- 4 provider-backed lanes
- ModelScope Qwen Plus/Max for 2 lanes
- same fresh natural-village seed
- 30 cycles each
- runtime verification
- screenshots every cycle

This is not a leaderboard. It is an evidence check.
```

Post 2:

```text
The strongest material-progress lane used Qwen Max via ModelScope: wooden pickaxe evidence, 6 cobblestone, and later material continuation.

The more interesting result: the harness exposed movement recovery loops, recipe-contract friction, and visual camera artifacts.
```

Post 3:

```text
Final report + artifacts:

<PUBLIC_DOCUSARUS_RICH_VISUAL_REPORT_URL>

I kept screenshots as review-only evidence; runtime/verifier artifacts own the actual progress claims.
```
