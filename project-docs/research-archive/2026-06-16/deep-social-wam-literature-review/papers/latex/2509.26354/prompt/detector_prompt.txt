You are a tool-packaging expert, we are now scaling our MCP Tools. Read the readme file of this project and a part of the core code, Please package the project to an MCP Tool.

Guidelines:
- If multiple candidates exist, pick one and proceed. But you SHOULDNOT package a single function into an MCP Tool, for example, if the readme shows that this project can do A, B and C, you should output a MCP Tool calling A or B or C, instead of packaging a single function ("def ....") in A
- In the "reason" field, you can output your reasoning process.
- Output ONLY valid JSON. No code fences, no extra text. Replace placeholders with real parameter names and descriptions.


***********************************
Attention: 
1. You should package the entire project as an MCP Tool. This must represent at least one complete feature as claimed in the Readme, not just a single, separate function. Note that the project is a cohesive whole; you cannot extract and package one part of its functionality in isolation.
2. You should mainly read the readme file instead of the code, the code is only for reference and for check.

File name: {file_name}

Tool content:
{tool_content}

You should follow the guidelines and attention to package the whole project into an MCP Tool.
You should mainly reference to readme to get the features of the project and package one feature into an Tool, which is not necessarily in the form of a function.

```Example
if the readme shows a feature and the code to call it like:
README:
......

python3 docker/run_docker.py \
  --fasta_paths=multimer.fasta \
  --max_template_date=2020-05-14 \
  --model_preset=multimer \
  --data_dir=$DOWNLOAD_DIR \
  --output_dir=/home/user/absolute_path_to_the_output_dir
 
 
......

you can package this feature directly into an MCP Tool.
```

THE EXAMPLE IS SIMPLY FOR ILLUSTRATION, YOU SHOULD NOT PACKAGE THE PROJECT IN VARIOUS WAYS.
*******************


Reply in JSON with this shape:
{json_template}