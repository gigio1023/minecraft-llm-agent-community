\section{How Agent Optimization Power Drives Misalignment}\label{sec:measurement}
%Continuing our case study of how reward hacking might emerge in a real-world RL agent, we now study reward hacking as a function of optimization power.

\begin{figure}[bpt]
\centering
\subfloat[Traffic - Ontological] {\includegraphics[scale=0.41]{figures/traffic/commute_red_max_proxy_single.eps}\label{fig:t_ont}}%
\hfill
\subfloat[COVID - Ontological] {\includegraphics[scale=0.41]{figures/pandemic/political_red_max_proxy_single.eps}\label{fig:p_ont}}%
\hfill
\subfloat[Glucose - Ontological] {\includegraphics[scale=0.41]{figures/glucose/insulin_max_proxy_single.eps}\label{fig:g_ont}}%

%\captionsetup{position=bottom}
\caption{Increasing the RL policy's model size decreases true reward on three selected environments. The red line indicates a phase transition.}
\label{fig:modelsize_scatter}
\end{figure}



To better understand reward hacking, we study how it emerges as agent optimization power increases. We define optimization power as the effective search space of policies the agent has access to, as implicitly determined by model size, training steps, action space, and observation space.

%We study optimization power along four capabilities / axes: parameter count, CPU-hours used during training, the control the agent has over its action space, and the fidelity of its environment observations. We freely interchange ``capabilities" and ``optimization power", both of which we use to represent the effective search space of policies the agent has access to, which is implicitly determined by the four factors aforementioned.
%We first instantiate the misspecifications outlined in Table~\ref{tab:misspecification_summary}. 
% We consider four different ways of measuring ``capabilities'' for an RL agent:
% %axes of optimization power along which RL agents have continued to grow: 
% parameter count, CPU-hours used during training, the control the agent has over its action space, and the granularity of its environment observations.

In Section~\ref{sec:quantitative}, we consider the quantitative effect of optimization power for all nine environment-misspecification pairs; we primarily do this by varying model size, but also use training steps, action space, and observation space as robustness checks. Overall, more capable agents tend to overfit the proxy reward and achieve a lower true reward. We also find evidence of phase transitions on four of the environment-misspecification pairs. For these phase transitions, there is a critical threshold at which the proxy reward rapidly increases and the true reward rapidly drops.

%After grounding our misspecifications and environments in reality, we now explore how reward hacking emerges as a function of optimization power. Given three types of misspecification and Given our three types of misspecification and four environments, we instantiate the misspecifications outline in Table~\ref{tab:misspecification_summary}.

 %Section~\ref{sec:qualitative} covers quantitative trends in reward hacking versus optimization power. We find that as stronger optimizers tend to overfit the proxy reward and achiever a lower true reward. We also find evidence of phase transitions in \textcolor{red}{N} of our environment-misspecification pairs. 
 
 In Section~\ref{sec:qualitative}, we further investigate these phase transitions by qualitatively studying the resulting policies. At the transition, we find that the quantitative drop in true reward corresponds to a qualitative shift in policy behavior. Extrapolating visible trends is therefore insufficient to catch all instances of reward hacking, increasing the urgency of research in this area.
 
 In Section~\ref{sec:correlation}, we assess the faithfulness of our proxies, showing that reward hacking occurs even though the true and proxy rewards are strongly positively correlated in most cases.  
 

 %shift our focus towards understanding the qualitative shifts in policy behavior that give rise to lower true reward. 
 %We observe that these shifts are often difficult to predict beforehand, increasing the urgency of research on reward hacking. %Towards understanding how misalignment arises, we use Section~\ref{sec:qualitative} to describe the qualitative shifts in behavior as model capabilities increase. These behavior shifts, especially those that are sudden, motivate the need for additional research into reward misspecification.%agents' optimization power increases, they overfit the proxy reward and achieve a lower true reward. 
%We provide quantiative results in Section~\ref{sec:quantitative} and contextual, qualitative results in Section~\ref{sec:qualitative}. 

% \kbcomm{I think the introduction to this section should outline what is going to happen in sections 3.1 and 3.2, and provide a very brief view into what kind of results the reader can expect going into these sections. Start off by saying that we now instantiate the misspecifications in the four different environments and consider several different notions of optimization ability: a, b, c, and d.} 

% \kbcomm{Section 3.1 highlight quantitative trends along these different axis. We find that ... In section 3.2, we shift our focus to studying what causes these quantitative trends. Qualitatively, we observe that ... }

% Each of our four environments has multiple desiderata that need to be balanced by the RL agent and reward designer. A reward designer will specify a reward function, and attempt to train a policy with the goal of maximizing the proxy reward. To improve the policy, a designer might attempt to increase the agent's optimization ability. Although there are several notions of optimization ability, we consider the number of parameters in the agent, the number of training steps, the control the agent has over its action space, and the granularity of its environment observations. 

%After a reward designer specifies a reward function, the  We need to ensure that In Section~\ref{sec:quantitative}, we quantitatively highlight how misalignment is worsened by increased model capabilities. Typically, as capabilities increase, the proxy reward increases and the true reward decreases. In particular, we find evidence of phase transitions in . %There are also environment-misspecification pairs that do not produce any misalignment. It is a priori unclear 