\section{Conclusion}

Using our \benchmark, we showed that top RMs on the standard RewardBench benchmark all display brittleness under minor meaning- or ranking-preserving input transformations.
We demonstrated a simple recipe to improve RM robustness through regularization, which not only improves RM consistency on \benchmark, but, when used in alignment, also leads to better outputs.
