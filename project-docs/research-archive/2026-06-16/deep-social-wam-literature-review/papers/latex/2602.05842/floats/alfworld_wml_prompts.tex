\begin{table}[t!]
    \caption{Prompt for \wmrlshort{} in ALFWorld, given a current state $s_t$ and an action $a_t$. Final output within the \texttt{<next\_state>} \texttt{</next\_state>} tags is used to compare with the next state in $r^{\textrm{WM}}(\hat{s}_{t+1}, s_{t+1})$ during RL.}
    \label{tab:alfred_wmrl_prompt}
    \begin{tabular}{p{16.5cm}}
    \toprule
    % Prompt \\
    % \midrule
    You are an expert agent operating in the ALFRED Embodied Environment. Your task is to ...\emph{\textcolor{gray}{// omitting task instruction and previous action history here for brevity}}
    \newline

    Your current observation is:
    \textcolor{blue}{\{}\textcolor{blue}{current\_state}\textcolor{blue}{\}}


    \textbf{Potential action:}
    \textcolor{blue}{\{}\textcolor{blue}{action}\textcolor{blue}{\}}
    \newline

    Now, your task is to predict the immediate next observation after taking the potential action above.

    You should first briefly reason step-by-step about the previous steps and current situation — summarize key information you've learned about the environment that is relevant to the task. This reflection and reasoning process \textbf{must} be enclosed within \texttt{<think>} \texttt{</think>} tags.

    Once you've finished your reasoning, you should describe the next observation (use the past and current observations as examples!) and present them within \texttt{<next\_state>} \texttt{</next\_state>} tags.
    \\
    \bottomrule
    \end{tabular}
\end{table}


\begin{table}[t!]
    \caption{Prompt for \wmsftshort{} in ALFWorld, given a current state $s_t$ and an action $a_t$. The label is the next state $s_{t+1}$.}
    \label{tab:alfred_wmsft_prompt}
    \begin{tabular}{p{16.5cm}}
    \toprule
    You are an expert agent operating in the ALFRED Embodied Environment. Your task is to ...\emph{\textcolor{gray}{// omitting task instruction and previous action history here for brevity}}
    \newline

    Your current observation is:
    \textcolor{blue}{\{}\textcolor{blue}{current\_state}\textcolor{blue}{\}}


    \textbf{Potential action:}
    \textcolor{blue}{\{}\textcolor{blue}{action}\textcolor{blue}{\}}
    \newline

    Now, your task is to predict the immediate next observation after executing the potential action above.

    Directly present your final prediction of the next observation (use the past and current observations as examples!) within \texttt{<next\_state>} \texttt{</next\_state>} tags. DO NOT generate anything else.
    \\
    \midrule
    Label: \texttt{<think>} \texttt{</think>}

    \texttt{<next\_state>} \textcolor{blue}{\{}\textcolor{blue}{next\_state}\textcolor{blue}{\}} \texttt{</next\_state>}
    \\
    \bottomrule
    \end{tabular}
\end{table}