# SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering

- **title**: SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering
- **authors**: John Yang, Carlos E. Jimenez, Alexander Wettig, Kilian Lieret, Shunyu Yao, Karthik Narasimhan, Ofir Press (Princeton)
- **year**: 2024 (NeurIPS 2024)
- **venue/source**: NeurIPS 2024; arXiv 2405.15793
- **arxiv_id**: 2405.15793
- **urls**: paper https://arxiv.org/abs/2405.15793 ; code https://github.com/princeton-nlp/SWE-agent ; hf https://huggingface.co/papers/2405.15793
- **source availability**: latex (deep-read abstract + ACI + experiments sections)

## Primary-source facts

- **Research question**: how much does INTERFACE design (not the model) determine an LM agent's software-engineering performance.
- **Core idea: the Agent-Computer Interface (ACI)**: LM agents are a new class of end-user that benefits from purpose-built interfaces, just as human engineers benefit from IDEs. The ACI specifies the commands the LM can issue and how environment state is reported back, and manages history/formatting at each step.
- **Four ACI design principles (stated)**: (1) actions simple and easy for agents to understand (few options, concise docs); (2) actions compact and efficient (consolidate file navigation/editing into few actions); (3) environment feedback informative but concise (report effect of recent actions without clutter); (4) guardrails mitigate error propagation and hasten recovery (e.g. an automatic code syntax checker catches edit mistakes). The ACI was tuned by manual behavior inspection on a dev set plus a grid search over window size, history processing, decoding temperature.
- **Results**: state-of-the-art on SWE-bench at the time, pass@1 12.5% (vs the previous best 3.8% from non-interactive RAG) and 87.7% on HumanEvalFix. Models = GPT-4 Turbo and Claude 3 Opus (chosen for 128k/200k context); smaller-context models (Llama 3 8k) were subpar. Baselines = non-interactive BM25-RAG and a Shell-only InterCode-style agent. Per-instance budget capped at $4 (edits auto-submitted on overrun); cost reported per resolved instance.

## Interpretation (flagged as inference)

- SWE-agent is the lane's clearest statement that the harness/interface is first-order: holding the LM fixed, a better ACI roughly tripled the SWE-bench resolve rate. This corroborates ENPIRE's "harness matters as much as the model" and MLE-bench's "scaffold dominates."
- The four ACI principles are a direct design spec for the repo's own action_surface: the repo already exposes the LLM a bounded set of typed tools (simple, compact actions), structured typed state and source-evidence (informative-but-concise feedback), and schema/permission/retry gates (guardrails that catch and recover from errors). SWE-agent gives external evidence that this is the right shape, not over-engineering.
- The guardrail principle (a syntax checker that auto-catches edit errors) is the SWE analog of the repo's runtime gates: the interface, not the model's good intentions, prevents and recovers from malformed actions.

## Mechanically useful vs research contribution

- **Mechanically useful**: the ACI design principles as an explicit checklist for the repo's tool/action_surface and gate layer; the guardrail-in-the-interface pattern (catch malformed actions at the interface, return a concise corrective signal); the budget cap with auto-submit on overrun (a clean loop-effort cap).
- **Not a contribution to copy**: building an ACI is engineering support; SWE-agent's research contribution is demonstrating that interface design measurably changes agent capability. The repo should adopt the principles, not claim "SWE-agent for Minecraft."
- **Honest bound**: SWE-agent's interface is for a deterministic, testable digital environment. The repo's Physical/Material action surface can be made equally crisp; social actions (request, promise, blame) are harder to give clean interface feedback for, which limits how far the ACI discipline carries up the WAM stack.

## WAM layer(s) informed

Method-level, cross-layer but cleanest at Physical/Material. Informs the repo's action_surface and gate design: the agent-computer interface is where capability is shaped, and guardrails belong in the interface, not in trusting the LLM.
