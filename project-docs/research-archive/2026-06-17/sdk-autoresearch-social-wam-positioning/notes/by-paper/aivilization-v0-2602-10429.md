# AIvilization v0 (2602.10429): a post-Sid large-society contrast

- Source: arXiv 2602.10429 (HKUST + Bauhinia AI, submitted 2026-02-11). Project
  site https://aivilization.ai.
- Access: WebSearch (abstract + project-site summary). CLAIM-ONLY: I did not fetch
  the body; release/scoring-artifact status NOT verified.
- Why in this lane: it shows the large-scale-society research line continues after
  Project Sid, so the repo's cautionary framing must be stated against a live trend,
  not a single 2024 outlier.

## What it claims (CLAIM-ONLY, abstract level)

- A publicly deployed large-scale artificial society coupling a resource-constrained
  sandbox economy with a unified LLM-agent architecture, built for long-horizon
  autonomy under a changing environment.
- Mechanisms claimed: a hierarchical branch-thinking planner that splits life goals
  into parallel objective branches with simulation-guided validation and tiered
  re-planning; an adaptive agent profile with dual-process memory (short-term traces
  vs long-term semantic consolidation); a human-in-the-loop steering interface.

## Interpretation for the repo (labeled inference)

- NOT a Minecraft / embodied-world substrate; it is a sandbox economy society. So it
  is a contrast for the SOCIAL-CLAIM scale axis, not for the physical-grounding
  axis. Useful only to show "large emergent-society narrative" is an ongoing genre.
- Whether AIvilization releases runnable scoring is unverified; do NOT cite it as
  either a positive or negative reproducibility example. Use it only to make the
  point: the repo's differentiation is verified small-N world-grounded transitions,
  not scale, and the field keeps producing scale-and-narrative systems that the repo
  must not imitate.
- Open question for a later lane: does AIvilization release a scoring harness? If
  yes, it would be a better-than-Sid reproducibility datapoint; if no, it joins Sid
  in the claim-only column. Left unresolved this pass.
